# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# מסד הנתונים הוא תוצר בנייה, לא מקור. הוא נבנה מ-data/seed/*.json:
#     python scripts/build_db.py
# קובץ בינארי שמשתנה בכל בנייה מנפח את היסטוריית הגיט בלי להוסיף מידע.
data/sofdavar.db
data/*.db-wal
data/*.db-shm

# מערכת הפעלה ועורכים
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# תוצרי ביניים של סבבי הניסוח לשאלות השבת. נשמרים מקומית כתיעוד של מה
# שנוסה ונפסל (העתקה מהספר, תוספת פסק), אך אינם נכסים של הפרויקט —
# הראיות שנדרשות לסקירת הטיוטות יושבות ב-data/review/.
draft_review*.json
draft_final.json
run566_final.json
data/seed/units_wanted.json

# פלטים גולמיים של סבבי הייבוא וההרחבה. התוכן שעבר את הבקרה כבר יושב
# ב-data/seed/questions.json, ולכן הקבצים האלה הם פיגומים ולא מקור.
book_import.json
expand_raw.json

# תלות אחת ויחידה של Node, ורק לבניית המצגת:
#     npm install  &&  node scripts/make_deck.mjs
# האתר עצמו אינו משתמש ב-Node כלל.
node_modules/
package-lock.json
