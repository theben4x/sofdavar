Flask>=3.0
Markdown>=3.5

# app/semantic.py ו-app/embed.py מייבאים numpy ברמת המודול. מקומית הוא
# מותקן ולכן הכול עבד, ובפריסה הוא חסר — הייבוא זרק ImportError,
# db.suggestions בלע אותו והחזיר רשימה ריקה, ומדור "עוד מהמאגר" נעלם
# מהאתר החי בלי שום סימן. נמדד: 3 הצעות מקומית מול 0 בפרודקשן.
numpy>=2.0

# zoneinfo נשען על מסד אזורי הזמן של IANA. ב-Windows/macOS הוא פשוט לא קיים,
# ובתמונות לינוקס רזות (כולל סביבות serverless) הוא לא מובטח. app/zmanim.py
# קורא ל-ZoneInfo("Asia/Jerusalem") *בזמן import*, ולכן חוסר שלו לא מפיל
# עמוד אחד אלא את כל האפליקציה. מותקן בכל פלטפורמה — 350KB זה ביטוח זול.
tzdata

# רק לסקריפט האימות scripts/verify_zmanim.py. האתר עצמו לא פונה לרשת.
requests>=2.31
