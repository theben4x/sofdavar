Flask>=3.0
Markdown>=3.5

# השכבה הסמנטית. שלושתם מיובאים ברמת המודול ב-app/embed.py ו-
# app/semantic.py, ואף אחד מהם לא היה כאן: מקומית הם מותקנים ולכן הכול
# עבד, ובפריסה הייבוא זרק ImportError, db.suggestions בלע אותו והחזיר
# רשימה ריקה, ומדור "עוד מהמאגר" נעלם מהאתר החי בלי שום סימן.
# נמדד: 3 הצעות מקומית מול 0 בפרודקשן.
#
# markupsafe מגיע ממילא כתלות של Flask, והוא כאן כי routes.py מייבא
# אותו ישירות — מה שמייבאים, מצהירים.
numpy>=2.0
tokenizers>=0.20
markupsafe>=2.1

# zoneinfo נשען על מסד אזורי הזמן של IANA. ב-Windows/macOS הוא פשוט לא קיים,
# ובתמונות לינוקס רזות (כולל סביבות serverless) הוא לא מובטח. app/zmanim.py
# קורא ל-ZoneInfo("Asia/Jerusalem") *בזמן import*, ולכן חוסר שלו לא מפיל
# עמוד אחד אלא את כל האפליקציה. מותקן בכל פלטפורמה — 350KB זה ביטוח זול.
tzdata

# רק לסקריפט האימות scripts/verify_zmanim.py. האתר עצמו לא פונה לרשת.
requests>=2.31
